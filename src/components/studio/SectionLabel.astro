---
interface Props {
  as?: 'h2' | 'p';
  id?: string;
}

const { as: Tag = 'h2', id } = Astro.props;
---

<Tag
  id={id}
  class="font-ui text-[10px] font-semibold uppercase tracking-[0.28em] text-ink-faint"
>
  <slot />
</Tag>
