---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { isLocale, type Locale } from '@/lib/constants';
import { t } from '@/lib/i18n/ui';
import { langStaticPaths } from '@/lib/i18n/routing';

export function getStaticPaths() {
  return langStaticPaths();
}

const { lang: raw } = Astro.params;
if (!isLocale(raw ?? '')) throw new Error('Invalid locale');
const lang = raw as Locale;
const path = Astro.url.pathname;
---

<BaseLayout title={t(lang, 'terms.title')} description="Terms placeholder." lang={lang} path={path} noindex>
  <header class="mb-8 max-w-3xl">
    <h1 class="font-display text-display-lg text-ink">{t(lang, 'terms.title')}</h1>
    <p class="mt-4 text-ink-muted">Placeholder terms page. Replace with your finalized legal text.</p>
  </header>
  <div class="prose-article max-w-measure text-ink-muted">
    <p>Content is provided for education and professional development. No warranty.</p>
  </div>
</BaseLayout>
